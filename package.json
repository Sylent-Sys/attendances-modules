{
  "name": "attendances-modules",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "migrate": "bun run src/migrations/run.ts"
  },
  "dependencies": {
    "@elysiajs/openapi": "^1.4.11",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
